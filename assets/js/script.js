/**
 * declare player choices and constants for dom elements
 */
const buttons = document.getElementsByClassName("control");
const player score = document.getElementById("player-score")
const computer score = document.getElementById("computer-score");
const player image = document.getElementById("player-image");
const computer image = document.getElementById("computer-image");
const message = document.getElementById("messages");
const choices = ["rock", "paper", "scissors"];